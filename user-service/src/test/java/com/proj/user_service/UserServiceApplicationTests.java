package com.proj.user_service;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest // This annotation loads the application context for testing
class UserServiceApplicationTests {

	@Test // This marks the method as a test case
	void contextLoads() {
		// This test passes if the application context can be loaded without errors.
	}
}